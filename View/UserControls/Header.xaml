<UserControl x:Class="L_system.View.UserControls.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:L_system.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="97.899" d:DesignWidth="800">
    <Grid Height="{Binding Height}" Background="Aqua">
        <Image Source="\View\Resources\Images\Chmonya.jpg" HorizontalAlignment="Left"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource HoverLess}" Padding="5">
                -
            </Button>
            <Button Style="{StaticResource HoverLess}" Padding="5">
                #
            </Button>
            <Button Style="{StaticResource HoverLess}" Padding="5">
                X
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
